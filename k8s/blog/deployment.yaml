# [MSA] blog-service Deployment + Service — K8s 독립 배포 단위
# [왜] Docker Compose에서 K8s로 승격: Self-healing + Rolling Update + HPA 지원
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-service
  namespace: kmsa
  labels:
    app: blog-service
    tier: apps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blog-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: blog-service
    spec:
      containers:
        - name: blog-service
          image: 2kijung/kmsa-blog-service:latest
          ports:
            - containerPort: 8086
          env:
            - name: DB_HOST
              value: postgres-service
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: kmsa
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: kmsa-secrets
                  key: db-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kmsa-secrets
                  key: db-password
            - name: ZIPKIN_HOST
              value: zipkin-service
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "400m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /blog/health
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /blog/health
              port: 8086
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: blog-service
  namespace: kmsa
spec:
  selector:
    app: blog-service
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP
