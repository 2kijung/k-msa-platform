# [MSA] analytics-service Deployment + Service — K8s 독립 배포 단위
# [왜] Docker Compose에서 K8s로 승격: Self-healing + Rolling Update + HPA 지원
apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: kmsa
  labels:
    app: analytics-service
    tier: apps
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analytics-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: 2kijung/kmsa-analytics-service:latest
          ports:
            - containerPort: 8087
          env:
            - name: DB_HOST
              value: postgres-service
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: kmsa
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: kmsa-secrets
                  key: db-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kmsa-secrets
                  key: db-password
            - name: ZIPKIN_HOST
              value: zipkin-service
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "400m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /analytics/health
              port: 8087
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /analytics/health
              port: 8087
            initialDelaySeconds: 20
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: kmsa
spec:
  selector:
    app: analytics-service
  ports:
    - port: 8087
      targetPort: 8087
  type: ClusterIP
