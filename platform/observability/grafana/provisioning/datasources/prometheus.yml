# Grafana 자동 데이터소스 프로비저닝
# [왜] 컨테이너 재시작 시 Prometheus 데이터소스 자동 연결.
#      수동 설정 없이 대시보드 즉시 사용 가능.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
